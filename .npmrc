# NPM configuration
# This file ensures proper npm authentication and publishing behavior

# Publish to public registry
registry=https://registry.npmjs.org/

# Use authentication token for automated publishing
# The token should be provided via NPM_TOKEN environment variable
# (set in GitHub Actions secrets)
